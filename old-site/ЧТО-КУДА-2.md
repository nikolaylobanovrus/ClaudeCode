# Обновление 2 (08.09.2026): иконки мессенджеров

Три файла. Два заменяют уже загруженные, один новый.

1. Панель Beget → «Файловый менеджер» → папка `nalog-service.online` → `public_html`.
2. В корень (где лежат `index.html`, `offer.html`): загрузить `index.html` с заменой.
3. В папку `js`: загрузить `app.bc237cf3.js` с заменой.
4. В папку `img` (там много картинок): загрузить `max-logo.svg`. Это новый файл,
   вопроса о замене не будет.
5. Открыть https://nalog-service.online и нажать Ctrl+F5.

Что должно измениться:
- в блоке «Задайте вопрос эксперту напрямую» две кнопки, Telegram и Max,
  стоят по центру; у Telegram вместо QR-кода логотип (старый QR вёл на
  номер 912, который больше не используется);
- у Max фирменная иконка, как в приложении;
- в «Контактах» иконки Telegram и Max по центру своей колонки.

Остальные файлы (offer.html, policy.html) не менялись, трогать не нужно.
