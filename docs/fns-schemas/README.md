# XSD-схемы ФНС для проверки XML-декларации

Здесь лежат официальные XSD-схемы формата 3-НДФЛ (файл обмена НО_НДФЛ3)
за 2022–2025 годы (версии формата 5.17–5.21). С их помощью скрипт
`npm run validate:xml` проверяет, что XML, который формирует сайт,
соответствует формату ФНС.

**Статус: XML проходит валидацию по всем годам и комбинациям вычетов**
(`npm run validate:xml` → всё OK), поэтому пометка «бета» с файла снята.
Схемы лежат в репозитории, чтобы проверку можно было повторить.

## Версия формата меняется и без смены года — следить обязательно

10.09.2026 выяснилось, что за 2025 год формат сменился с 5.20 на **5.21**
посреди года: приказ ФНС от 25.05.2026 № ЕД-1-11/333@ внёс изменения в
приложения к приказу ЕД-7-11/913@, в силе с 01.09.2026. Мы это пропустили на
девять дней и всё это время отдавали клиентам файлы с `ВерсФорм="5.20"`.

Почему `npm run validate:xml` молчал: он сверяет XML со схемой, лежащей
**в этой папке**, а там оставалась старая. Валидатор проверяет соответствие
нашего генератора схеме, но не проверяет, что схема — действующая.

Цена ошибки высокая: в схеме `ВерсФорм` — перечисление ровно из одного
значения (`<xs:enumeration value="5.21"/>`), поэтому файл со старым номером
не проходит проверку вообще, независимо от содержимого.

Что изменилось в 5.21 по сравнению с 5.20 (полный дифф — три пункта):

- новый атрибут `ВычСбер5.1.219.2` — вычет на долгосрочные сбережения,
  `use="optional"`;
- новое значение `5` в перечислении `ВидДоговор`;
- `ВерсФорм`: `5.20` → `5.21`.

Ничего не удалено и ничего не стало обязательным, поэтому наш генератор
подошёл под новую схему без структурных правок — хватило смены константы
`xmlVersion` в `src/lib/ndfl/refs.js`. Сам вычет на долгосрочные сбережения
(строки 235–280, элемент `РасчВычСбер`) добавлен отдельно 10.09.2026 — см.
`docs/fns-blank-2025.md`; там же таблица «строка → подпункт → атрибут».

## Схема молчит о том, чего в файле нет

Валидатор проверяет соответствие того, что мы выгрузили. Если новый код не
сработал вовсе — потому что в тестовом черновике не было нужных данных, —
файл всё равно пройдёт схему. За разработку это выходило трижды подряд,
поэтому в `scripts/validate-3ndfl-xml.mjs` есть таблица `MUST_CONTAIN`:
для базового сценария каждого года перечислены элементы, которые обязаны
оказаться в XML. Добавляете новый вычет — впишите его элемент туда, иначе
проверка останется пустой.

Первоисточник (сверено 10.09.2026 постранично): **Приложение № 4 к приказу
ФНС от 25.05.2026 № ЕД-1-11/333@** «Формат представления налоговой
декларации по налогу на доходы физических лиц (форма 3-НДФЛ) в электронной
форме», 67 страниц, таблицы 4.1–4.63. Стр. 1: «Номер версии настоящего
формата 5.21, часть XXXIII». Стр. 2: «NO_NDFL3_1_033_00_05_21_xx»,
первая строка XML — `<?xml version="1.0" encoding="windows-1251"?>`.
Файл `format_ed-1-11-333_data_25.05.2026.pdf` — у владельца; в репозиторий
не кладём (3,3 МБ скана против 13 МБ всей истории), нормативным артефактом
здесь считается XSD.

**Регламент:** при каждом приказе ФНС по 3-НДФЛ проверять раздел форм на
nalog.gov.ru и обновлять схему здесь. Проверка версии формата вынесена в
ежемесячный отчёт.

**Пятерым покупателям с 01.09 по 10.09 рассылки не делаем.** Решение
владельца от 11.09.2026: новые файлы отправляем, только если ФНС развернёт
декларацию и человек вернётся к нам. Основание — вероятный переходный
период, в котором приёмный комплекс принимает и старую версию формата.
Рассылка «у вас неправильный файл» тем, у кого всё прошло, создаёт проблему
на ровном месте. Заготовка письма поэтому не нужна до первого возврата.

Схемы и текст формата качает владелец: из облака `format.nalog.ru` и
`publication.pravo.gov.ru` отдают 503, `data.nalog.ru` — 403. Архивы ФНС
бывают в RAR; в песочнице его открывает `unar` (`apt-get install -y unar`),
7-Zip из Ubuntu не годится — RAR-кодек в нём вырезан как несвободный.

## Почему схемы не в репозитории

Сайт работает в облаке, а сервер форматов ФНС (`format.nalog.ru`,
`data.nalog.ru`) закрывает автоматический доступ (ошибка 403). Поэтому
скачать схемы автоматически из среды разработки нельзя — их нужно один
раз положить сюда вручную из вашей сети.

## Как скачать схемы (5 минут)

1. Откройте страницу форм 3-НДФЛ на сайте ФНС:
   https://www.nalog.gov.ru/rn77/taxation/taxes/ndfl/form_ndfl/
2. Для нужного года откройте приложение «Формат представления… в
   электронной форме» — там будет ZIP-архив со схемой (`.xsd`).
   Прямые имена схем по годам:
   - 2022 → `NO_NDFL3_1_033_00_05_17_xx.xsd` (приказ ЕД-7-11/880@)
   - 2023 → `NO_NDFL3_1_033_00_05_18_xx.xsd` (приказ ЕД-7-11/903@/615@)
   - 2024 → `NO_NDFL3_1_033_00_05_19_xx.xsd` (приказ ЕД-7-11/757@)
   - 2025 → `NO_NDFL3_1_033_00_05_21_xx.xsd` (приказ ЕД-7-11/913@
     **в ред. от 25.05.2026 № ЕД-1-11/333@**, действует с 01.09.2026)
   (`xx` — номер версии схемы, берите самый свежий.)
3. Распакуйте архив и положите файлы схемы в эту папку, ПЕРЕИМЕНОВАВ
   главный файл по году — ровно так:
   - `docs/fns-schemas/3ndfl-2022.xsd`
   - `docs/fns-schemas/3ndfl-2023.xsd`
   - `docs/fns-schemas/3ndfl-2024.xsd`
   - `docs/fns-schemas/3ndfl-2025.xsd`
   Вспомогательные файлы (общие типы, справочники — например
   `NO_NDFL3...`, `Common...`), если схема на них ссылается, положите
   рядом БЕЗ переименования — `xmllint` подхватит их по относительным
   путям из главного файла.

Класть можно не все годы сразу — валидатор проверит те, для которых
схема есть, а остальные пропустит.

## Как запустить проверку

```
npm run validate:xml
```

Скрипт сгенерирует тестовую декларацию за каждый год (через тот же код,
что и на сайте) и проверит её через `xmllint --schema`. Вывод — по годам:
`OK` (файл прошёл схему) или список ошибок с номерами строк.

## Как снять пометку «бета»

Когда `npm run validate:xml` показывает `OK` по всем нужным годам —
структура XML соответствует схеме ФНС. После этого в
`src/wizard/steps/StepDocuments.jsx` уберите проп `beta: true` у документа
«Файл для Личного кабинета ФНС» (это одна строка), и бейдж «бета» пропадёт.

Скорее всего валидатор сначала покажет ошибки: текущий генератор
формирует XML по своей схеме имён, а официальные имена элементов другие.
Их полный список и таблица соответствия — в `xml-structure.md` в этой же
папке (структура сверена с приказом ФНС ЕД-7-11/757@). По ошибкам
`xmllint` и этой таблице генератор `src/lib/ndfl/xml3ndfl.js` приводится к
официальному дереву — после чего валидация зеленеет и «бета» снимается.

> Схемы ФНС распространяются как официальные документы; при желании
> добавьте `docs/fns-schemas/*.xsd` в `.gitignore`, если не хотите держать
> их в репозитории — валидацию можно запускать локально.
